{
  "mcpServers": {
    "multi": {
      "command": "python",
      "args": ["-m", "multi_mcp"],
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}"
      },
      "cwd": "${HOME}/.local/share/hive/multi_mcp"
    }
  }
}
